循环服务器 : 在同一时刻只能响应一个客户端的请求
开发服务器 : 。。。。。。。。。多个。。。。。。


一  循环服务器 ： UDP服务器
socket(...);
bind(...);
while(1){
    recvfrom(...);
    process(...);
    sendto(...);    
}

二 循环服务器  TCP服务器
    socket(...);
    bind(...);
    listen(...);
    while(1){
        accept(...);
    }
    while(1){
        read(...);
    }
    while(1){
        process(...);
        write(...);
        close(...);
    }
