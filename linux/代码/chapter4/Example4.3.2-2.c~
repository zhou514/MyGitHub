#include <stdio.h>
    int main(int argc，char **argv){
        int i=0;   //代表要加的数
        int sum=0;//代表最终的和    
        do{
          sum+=i++;
        }while(i<10);
        printf("--- %d\n"，sum);
    }
