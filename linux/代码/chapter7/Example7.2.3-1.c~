int main(int argc,char **argv){
FILE *fp=popen("date","r");
if (fp==NULL){
printf("popen err\n");
return 0;
    	}                            
       	char buf[256]={0};           
       	fread(buf,256,1,fp);         
       	pclose(fp);                  
       	printf("---%s\n",buf);       
}
